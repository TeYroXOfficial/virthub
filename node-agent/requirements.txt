fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
psutil==6.1.1

# Powiązań libvirt celowo tu nie ma. Pakiet z PyPI kompiluje się wobec
# systemowego libvirt i wywala się, gdy libvirt jest nowszy od powiązań
# (np. Debian 13). Na węźle KVM instalator bierze systemowy python3-libvirt,
# który zawsze pasuje do zainstalowanej wersji, i tworzy środowisko z
# --system-site-packages. Węzeł kontenerów (Incus) libvirt nie potrzebuje wcale.
